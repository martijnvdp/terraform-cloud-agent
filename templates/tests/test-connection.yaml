apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tfc-agent.fullname" . }}-test-connection"
  labels:
    {{- include "tfc-agent.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tfc-agent.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
